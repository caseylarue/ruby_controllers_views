<div class="container">
	<div class="row">
		<h1>There are all of the comments</h1>
		<% if @comments %>
			<% @comments.reverse.each do |comment| %>
				<div>
					<p><%= comment.comment %> | <%= comment.product_name %> | <%= comment.created_at.strftime('%m/%d/%y %I:%M %p') %></p>
				</div>
			<% end %>
		<% end %>
		<%= link_to 'Back', products_path %>
	<div>
</div>